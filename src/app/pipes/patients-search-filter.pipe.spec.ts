import { PatientsSearchFilterPipe } from './patients-search-filter.pipe';

describe('PatientsSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientsSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
